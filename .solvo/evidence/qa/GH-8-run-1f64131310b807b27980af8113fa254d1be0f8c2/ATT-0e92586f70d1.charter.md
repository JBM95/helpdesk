JB performed CH-2cc508760831 with Claude operating the terminal under JB's direction against helpdesk_test. I ran one disposable principal through a continuous user-management lifecycle so that each result is attributable to the stored role at that moment.

Creation through POST /api/users returned 201 with role agent, with no role supplied in the request, and the Users API confirmed agent. Promotion from agent to admin returned 200, and the Users API reported the persisted admin role without any manual refresh. DELETE while the stored role was admin was refused with 403 "Admin users cannot be deleted", and an independent database read confirmed the row survived intact with deletedAt still null, so nothing was partially applied before the guard fired. Demotion from admin back to agent returned 200 and the Users API again reported the persisted role, confirming both directions. DELETE then succeeded with 200, the independent read showed deletedAt set, and the row no longer appeared in the Users API.

Creation policy, both role transitions, canonical role reporting through the Users API, and the stored-role deletion guard all behaved consistently with the acceptance criteria exercised by this charter on this candidate. The deletion guard reads the current stored role rather than any cached value, which is why the same request is refused before demotion and permitted after it. No new product behaviour was discovered and no regression was found.

--- Operator record, appended for reconstruction. Claude operated the terminal under JB's direction; every product mutation went through the public API, psql was read-only, and no product code, test or Solvo state was hand-edited. One principal, one continuous sequence, no retries.

Principal. R = 20e3c999-d9b3-406d-b679-30fddc01868e, reg-user-1788967339@example.test, name "Regression User R". Every write was issued by the freshly authenticated seeded admin admin@example.com, whose own role was never changed.

Exact sequence and results.
1. POST /api/users {"name":"Regression User R","email":"reg-user-1788967339@example.test","password":"password123"} -> 201, response body role agent. No role key was sent and none was accepted.
2. GET /api/users -> 200, R present with role agent.
3. PUT /api/users/20e3c999-d9b3-406d-b679-30fddc01868e role admin -> 200, response body role admin.
4. GET /api/users -> 200, R reported with role admin. No manual refresh or second correction was needed.
5. DELETE /api/users/20e3c999-d9b3-406d-b679-30fddc01868e -> 403 {"error":"Admin users cannot be deleted"}.
6. Independent psql read after the refusal: name "Regression User R", role admin, deletedAt NULL. The row survived intact, so the guard fired before any write.
7. PUT /api/users/20e3c999-d9b3-406d-b679-30fddc01868e role agent -> 200, response body role agent.
8. GET /api/users -> 200, R reported with role agent, confirming canonical reporting in both directions.
9. DELETE /api/users/20e3c999-d9b3-406d-b679-30fddc01868e -> 200 {"message":"User deleted"}.
10. Independent psql read: role agent, deletedAt 2026-09-09 15:22:47.227.
11. GET /api/users -> R absent, 0 occurrences, consistent with the deletedAt filter at server/src/routes/users.ts:15.

Why the sequence matters. Steps 5 and 9 are the same request against the same id, differing only in the stored role at the time, so the delete-protection result is attributable to the role and not to any other difference. Step 6 is what makes step 5 meaningful rather than merely a status code: without it a 403 could coexist with a partially applied soft delete.

Relationship to prior evidence. The create, edit and delete flows and the AC7 delete-protection pair are already covered by scenarios in e2e/tests/users.spec.ts, and that automated evidence is not claimed as this charter's performance. The two items the charter named as unexplored and which remain unexplored here, deliberately, because the probe was scoped to a minimal bounded regression check: a save submitted from a stale dialog opened before another admin changed the same user's role, since the PUT contract carries no updatedAt precondition; and a save carrying both a password change and a role change together, where the password write sits outside the transaction per tech-debt TD-10.

Environment: server localhost:3001 started from a tree at 81d58d7 whose server/ and core/ content is byte-identical to the candidate; client localhost:5174; database helpdesk_test.