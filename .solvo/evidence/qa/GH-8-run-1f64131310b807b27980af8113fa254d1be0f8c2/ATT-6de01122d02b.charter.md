JB performed CH-de866031b70f with Claude operating the terminal under JB's direction against helpdesk_test. I exercised bounded mutual-demotion concurrency, post-demotion session behavior, and product-level recovery using two disposable admins while keeping the seeded admin untouched as the recovery authority. Of three bounded race attempts, one was a valid mutual-demotion race and two were void because of harness defects and are not treated as race evidence. In the valid race, A's demotion of B returned 200 while B's competing demotion of A returned 401. The successful demotion removed B's session inside the same transaction, so the competing request failed at requireAuth before reaching requireAdmin. A later controlled state with both disposable principals demoted to agent showed that both could re-authenticate with their existing credentials but received 403 from the admin-only Users API and from attempts to promote themselves or each other. No refused attempt changed persisted roles. Configuration has disableSignUp: true, and POST /api/users is itself admin-only and creates agents, so there is no product-level recovery from a true zero-admin state. Code inspection found no admin-count or admin-floor check, so the presence of the untouched seeded admin is mechanically irrelevant to the two disposable-admin transactions. I therefore observed that global zero-admin is reachable in principle from the code, but I did not empirically observe the final-two race landing in this bounded experiment. The practical race window is narrower than static inspection alone suggested because a winning demotion destroys the losing principal's session before its authorization read can complete.

--- Operator record, appended for reconstruction. Claude operated the terminal under JB's direction; every product mutation went through the public API, psql was read-only, and no product code, test or Solvo state was hand-edited.

Setup. Seeded admin 86727e0a-8b95-4961-b574-48c577bbe269 (admin@example.com) untouched throughout, retaining one session as recovery authority. Disposable principals created via POST /api/users and promoted via PUT by the seeded admin: A = 78fa145c-16f6-4773-8166-64006f49eb64 (floor-a-1788965005@example.test), B = 833d6b38-a267-427f-bb06-8896232e2460 (floor-b-1788965005@example.test). Both authenticated with their own credentials and both reached GET /api/users with 200 before the race. Pre-race admin count 3.

Attempt 1 — VOID, not race evidence. Harness defect: shell '&' binds looser than '&&', so the BETA request launched in a context where the id/email variables were unset and never issued a valid write. Outcome was a plain sequential demotion of B by A. A's row was untouched at its promotion timestamp.

Attempt 2 — VALID race, missed. Two curl processes launched as background jobs. T0 launch 1788965130288, window 412ms. ALPHA (A demotes B) start 1788965130341, HTTP 200, starttransfer 0.0808, total 0.0809. BETA (B demotes A) start 1788965130361, HTTP 401 {"error":"Unauthorized"}, starttransfer 0.1454, total 0.1455. Approximately 20ms of process-spawn skew between the two issues. After: A admin (updatedAt unmoved at 14:43:37.245), B agent (14:45:30.526), B sessions 0. BETA's 401 rather than 403 is the measured result: the winner's commit dropped the loser's session in the same transaction, so the loser failed at requireAuth and never reached requireAdmin.

Attempt 3 — VOID, not race evidence. Single-process harness: curl -Z --parallel-immediate with two --next sections. Concurrency itself succeeded, with sub-millisecond issue skew (pretransfer 0.002052 and 0.000365 against ~20ms in attempt 2). The defect was identity, not timing: curl's cookie engine is per-process rather than per --next section, so two -b loads of the same cookie name (better-auth.session_token) merged and both requests carried B's session. ALPHA therefore returned HTTP 403 {"error":"You cannot change your own role"} — reachable only when req.user.id equals the target — and BETA returned 200 demoting A. The cookie jars on disk were correct and distinct (A: nxKuu2qn..., B: 7eka3nSy...). Incidental valid observations: the self-role guard fires correctly under concurrent load, and a non-self demotion succeeds normally. Methodological note for any repeat: pass identity as an explicit per-section -H 'Cookie: ...' header, which is not merged, rather than -b.

Retry cap of three was set by JB in advance and was not raised. Zero disposable admins was not observed in any valid attempt.

P2, existing-session aftermath, no re-login. A (demoted): GET /api/users 401 {"error":"Unauthorized"} and GET /api/me 401, with 0 session rows — the session was destroyed, not downgraded. B (never demoted): GET /api/users 200 and GET /api/me 200 with role admin. The session drop is scoped to the demoted user and did not affect the other admin.

P3, product recovery from a constructed zero-disposable-admin state. Because the valid race missed, the state was constructed rather than raced: the seeded admin demoted B via PUT, giving admin count 1 (seeded admin only) with A and B both agent and both at 0 sessions. Both re-authenticated with unchanged credentials (200 each, role agent in both payloads), so credentials survive a demotion and only sessions are destroyed. All six recovery attempts returned 403 {"error":"Forbidden"}: A GET /api/users; A promotes A; A promotes B; B GET /api/users; B promotes B; B promotes A. Every refusal came from requireAdmin, so the request never reached the handler and the self-role guard was not consulted. Final persisted roles unchanged, with both updatedAt values still at their demotion timestamps (A 14:52:23.173, B 14:55:24.761), so nothing was partially written. Signup as a recovery path is closed by configuration: server/src/lib/auth.ts:14 sets disableSignUp: true, and the only account-creation route, POST /api/users, is itself behind requireAdmin and hardcodes role agent at users.ts:45.

P4 reasoning recorded as reasoning, not as observation. Reachability in principle rests on three code facts: no admin-count check exists anywhere in non-generated server/src or core (the only grep hits are the acknowledging comment at users.ts:88-90); the two competing transactions update disjoint rows, so Postgres has nothing to serialise between them; and sequential paths are genuinely closed, since self-demotion is refused at users.ts:91 and a demoted admin loses both role and session, so any roster walks down to exactly one admin sequentially and global zero requires the final two to demote each other concurrently. That is structurally the A/B experiment, and the seeded admin's presence is mechanically irrelevant because no count is read and neither transaction touches its row. The gap between reachable-in-principle and observed is therefore a timing question that remains open, and it is closable without touching the seeded admin.

Environment: server localhost:3001 started from a tree at 81d58d7 whose server/ and core/ content is byte-identical to the candidate; client localhost:5174; database helpdesk_test, reset and reseeded immediately before this charter began. helpdesk_test is to be restored through the canonical Prisma reset + seed path after this evidence is recorded.