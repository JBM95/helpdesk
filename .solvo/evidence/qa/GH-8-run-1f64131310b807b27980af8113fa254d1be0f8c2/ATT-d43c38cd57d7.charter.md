JB performed CH-5089a9bf18a8 with Claude operating the terminal under JB's direction against helpdesk_test. I exercised both privilege transitions against sessions that already existed before the transition, with no re-authentication at any point after the initial sign-in.

For the demotion arm, a disposable principal was promoted to admin by the seeded admin, signed in, and confirmed to reach the admin-only Users API with 200. The seeded admin then demoted it to agent. The same session, reused without re-login, returned 401 Unauthorized from GET /api/users and also 401 from GET /api/me, and an independent database read showed the principal at role agent with zero session rows. Admin authorization was therefore lost immediately, and the loss is total rather than a downgrade: the session no longer authenticates at all.

For the promotion arm, an authenticated agent principal was confirmed to receive 403 Forbidden from GET /api/users while holding one session. The seeded admin promoted it to admin. The same session, reused without re-login, returned 200 from GET /api/users and 200 from GET /api/me reporting role admin, with the session count still one. Admin API access was therefore gained on the pre-existing session under the application's normal session rules, with no re-authentication required.

The asymmetry is the substantive observation and both halves behaved consistently with the current implementation and session rules: demotion drops the principal's sessions in the same transaction as the role write, while promotion deliberately leaves them intact. The resulting 403-before / 401-after pair means a demoted principal is distinguishable from a merely under-privileged one. No new product behaviour was discovered.

--- Operator record, appended for reconstruction. Claude operated the terminal under JB's direction; every product mutation went through the public API, psql was read-only, and no product code, test or Solvo state was hand-edited. Probe held to the two arms above.

Principals. Seeded admin admin@example.com performed every role write, and its own role was never changed. D (demotion subject) = 6aff66bc-07a6-4b90-97f4-5f0ad285c7b1, st-admin-1788967179@example.test, name "Session Admin D", created via POST /api/users as role agent (201). G (promotion subject) = 757e1430-9f74-4f8e-a0af-9dddbfb740fb, pb-agent-1788966985@example.test, name "Agent Principal G", carried over from CH-e0ea51e09abb where it was created as an agent and already held one authenticated session.

Demotion arm, exact sequence. PUT /api/users/6aff66bc-07a6-4b90-97f4-5f0ad285c7b1 role admin by seeded admin -> 200. Sign-in D -> 200. D's own session GET /api/users -> 200. psql: role admin, 1 session row. PUT /api/users/6aff66bc-07a6-4b90-97f4-5f0ad285c7b1 role agent by seeded admin -> 200. D's SAME cookie jar, no re-login: GET /api/users -> 401 {"error":"Unauthorized"}; GET /api/me -> 401 {"error":"Unauthorized"}. psql: role agent, 0 session rows.

Promotion arm, exact sequence. G's existing session GET /api/users -> 403 {"error":"Forbidden"}; psql: role agent, 1 session row. PUT /api/users/757e1430-9f74-4f8e-a0af-9dddbfb740fb role admin by seeded admin -> 200. G's SAME cookie jar, no re-login: GET /api/users -> 200; GET /api/me -> 200 with body user.role admin. psql: role admin, 1 session row — the count is unchanged, so no session was dropped or recreated.

What makes the transition claim real rather than incidental. In both arms the identical cookie jar was reused across the transition and no sign-in occurred between the role write and the follow-up request, so the change in outcome is attributable to the role write alone. The demotion arm's 401 on GET /api/me is the element that distinguishes session destruction from privilege downgrade; the promotion arm's unchanged session count is the element that shows privilege gain did not depend on a new session. Mechanically this rests on requireAuth calling auth.api.getSession() as a live joined read (server/src/middleware/require-auth.ts:6) so role is re-read per request, and on the demotion branch at server/src/routes/users.ts:110-120 batching prisma.session.deleteMany into the same transaction as the role update while promotion adds no such delete.

Relationship to prior evidence. e2e/tests/users.spec.ts already asserts both transitions against live sessions in its AC4/AC5 describe. That automated evidence is not claimed as this charter's performance; the two arms above were performed by hand under this charter on candidate 1f64131310b807b27980af8113fa254d1be0f8c2. Not exercised here, and left as known-unexplored: a third simultaneous session for one principal, and whether a promoted principal's UI gains the Users navigation entry without a reload.

Environment: server localhost:3001 started from a tree at 81d58d7 whose server/ and core/ content is byte-identical to the candidate; client localhost:5174; database helpdesk_test.