JB performed CH-e0ea51e09abb with Claude operating the terminal under JB's direction against helpdesk_test. Using an authenticated non-admin agent principal, I confirmed direct admin-only access is denied at the API: GET /api/users returned 403 Forbidden and PUT /api/users/:id attempting a promotion returned 403 Forbidden, with an independent database read confirming the target's role unchanged.

I then closed the AC3 proof gap the verifier recorded. The existing automated scenarios assert only that the stored role is unchanged after a rejected payload, so I issued two rejected updates that each also changed name and email. With an unsupported role value, the request returned 400 "Role must be either agent or admin". With the role key omitted entirely, it returned the same 400. After each, an independent database read showed name, email and role all unchanged, and updatedAt unmoved from its creation timestamp, so no partial write occurred. No row carrying either mutated email exists in the table.

This establishes that AC3's requirement to reject "without changing the stored user" holds for the relevant mutable fields exercised by this endpoint — name, email and role — and not merely for the role field. The unchanged updatedAt further supports that no partial write occurred. Validation runs before the target is loaded and before any write, consistent with server/src/routes/users.ts:74-75. No new product behaviour was discovered and no unrelated fuzzing was performed.

--- Operator record, appended for reconstruction. Claude operated the terminal under JB's direction; every product mutation went through the public API, psql was read-only, and no product code, test or Solvo state was hand-edited. Scope was held to the two arms above; no unrelated fuzzing.

Setup. Database freshly reset and reseeded immediately before this charter. Seeded admin admin@example.com signed in fresh (200). Two disposable agents created via POST /api/users, both 201 with role agent as created: T (target) = 5c0f7664-1d82-47e5-bdf6-307e1a1a98d8, pb-target-1788966985@example.test, name "Target User"; G (non-admin principal) = 757e1430-9f74-4f8e-a0af-9dddbfb740fb, pb-agent-1788966985@example.test.

Arm 1, authenticated agent denied admin-only access. G signed in 200 with role agent. G GET /api/users -> 403 {"error":"Forbidden"}. G PUT /api/users/5c0f7664-1d82-47e5-bdf6-307e1a1a98d8 with role admin -> 403 {"error":"Forbidden"}. Independent psql read of T afterwards: name "Target User", email pb-target-1788966985@example.test, role agent — unchanged. Both refusals come from requireAdmin at server/src/middleware/require-admin.ts, so the handler is never entered.

Arm 2, AC3 gap closure. Baseline for T from psql: name "Target User", email pb-target-1788966985@example.test, role agent, updatedAt 2026-09-09 15:16:26.005.
R1 body {"name":"MUTATED NAME","email":"pb-mutated-1788966985@example.test","password":"","role":"superuser"} -> HTTP 400 {"error":"Role must be either agent or admin"}. Independent psql read-back: name "Target User", email pb-target-1788966985@example.test, role agent, updatedAt 2026-09-09 15:16:26.005 — all three fields unchanged and updatedAt unmoved.
R2 body {"name":"MUTATED AGAIN","email":"pb-mutated2-1788966985@example.test","password":""} with the role key omitted entirely -> HTTP 400 {"error":"Role must be either agent or admin"}. Independent psql read-back identical, updatedAt still 2026-09-09 15:16:26.005.
Final check: SELECT count(*) FROM "user" WHERE email LIKE 'pb-mutated%' returned 0, so neither mutated email landed on any row.

Why this is not redundant with the automated suite. The six AC3 scenarios in e2e/tests/users.spec.ts each pair their 400 with a storedRole read only, which cannot distinguish "nothing was written" from "role was not written but name and email were". These two probes carried name and email changes alongside the bad role and proved both survived; the unmoved updatedAt is the strongest single element of that, since any partial write would have moved it. This is the gap recorded as FIND-527a2fe1941a.

Relationship to prior review evidence. Round 5 of the dev cycle probed role type confusion, mass assignment and prototype pollution against a live server. That is prior evidence and discharges no part of this charter; nothing from it is claimed here. The two arms above were performed under this charter, on candidate 1f64131310b807b27980af8113fa254d1be0f8c2.

Environment: server localhost:3001 started from a tree at 81d58d7 whose server/ and core/ content is byte-identical to the candidate; client localhost:5174; database helpdesk_test.