JB performed CH-3590ccb0991e with Claude operating the terminal under JB's direction against helpdesk_test. JB observed every raw result and stated the conclusion recorded here; Claude executed only the steps JB authorised, made no product-code or test changes, and made no direct SQL writes — psql was read-only for observation and every charter mutation went through the public API.

I exercised role mutation and deletion behavior separately for both principal kinds excluded from the normal user-management surface.

For a disposable soft-deleted user, I confirmed the row disappeared from GET /api/users, then successfully updated that same hidden row through PUT /api/users/:id to role=admin while deletedAt remained set. The row stayed invisible to the normal user-management surface. A subsequent DELETE /api/users/:id was refused with 403 "Admin users cannot be deleted".

For the ai-agent pseudo-user, the initial update using its seeded two-character name was rejected by ordinary name validation rather than by any protected-principal rule. I then repeated the probe with a temporary schema-valid name while keeping the email unchanged. PUT /api/users/ai-agent succeeded and persisted role=admin. The AI row remained excluded from GET /api/users and GET /api/agents. Ticket/stat endpoints continued returning successfully, although the database contained no tickets so this only proved they did not error, not that all downstream semantics were unaffected. While the AI row held role=admin, DELETE /api/users/ai-agent was refused with the same 403 "Admin users cannot be deleted".

The probes established that PUT /api/users/:id reaches both principal kinds excluded by the normal listing filters, and that after promotion the generic admin deletion guard protects those rows based solely on their stored role. No product code or tests were changed.

Raw sequence, for reconstruction. Soft-deleted arm: POST /api/users -> 201 id eb9e6f0f-3c19-4d54-be67-f577089f8dc7 role agent; DELETE -> 200; GET /api/users -> 0 matches; PUT role=admin with own name/email -> 200 role admin, deletedAt retained 2026-09-09 14:19:39.361; GET /api/users -> still 0 matches; DELETE -> 403 Admin users cannot be deleted. AI arm: original row id ai-agent name 'AI' (length 2) email ai@helpdesk.local role agent deletedAt null; PUT with own name -> 400 Name must be at least 3 characters, role unchanged; PUT with name 'AI Agent', email unchanged, password '', role admin -> 200, role admin persisted, updatedAt 2026-09-09 14:23:40.272; GET /api/users, GET /api/agents, GET /api/tickets, GET /api/tickets/stats, GET /api/tickets/stats/daily-volume all 200 with the AI row still excluded from both listings; DELETE while admin -> 403 Admin users cannot be deleted, row unchanged.

Evidence limitation stated by JB: downstream ticket/stat endpoints did not error after AI promotion, but the test database contained no tickets, so this does not establish absence of all functional impact. Environment: server localhost:3001 started from a tree at 81d58d7 whose server/ and core/ content is byte-identical to the candidate; client localhost:5174; database helpdesk_test at seed state (2 rows) when the charter began. helpdesk_test is to be restored by the normal Prisma reset + seed path after this evidence is recorded, because the API cannot restore the AI row's original two-character name.