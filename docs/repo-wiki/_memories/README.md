# _memories/ — the project's long-term memory

Story memories are written at the PR phase on the cycle branch; bug workflows write `bugfix-*.md` post-mortems. `_capability-index.md` is hand-maintained today, and no generator exists (AFSP-560). Read by `/dev-ticket` recon. Follows the obsidian-conventions skill — every memory is a vault node linked to its story, domain, and feature. Do not hand-edit the memories the workflows write; add human notes as separate linked notes.
